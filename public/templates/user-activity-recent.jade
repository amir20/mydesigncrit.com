- each item in activity
  - if(item.pages.length > 0)
    li
      a(href="/edit/#{item._id}", title="#{item.pages[0].title} from #{item.pages[0].url}")
        i.icon-folder-open
        |  #{item.pages[0].title}